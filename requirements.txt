# KNDB runtime dependencies — pinned. This is the full import closure of the
# app's ten direct deps (fastapi, uvicorn, sqlalchemy, aiosqlite, httpx,
# beautifulsoup4, pypdf, PyYAML, python-multipart, jinja2) plus their hard
# transitive deps; nothing installed here is unreachable from the code, and
# nothing the code imports is missing (validated by installing the file in a
# fresh venv and running the test suite against it).
#
# Deliberately absent:
#   - optional extras the app never turns on — uvicorn[standard] (httptools,
#     websockets, watchfiles, uvloop, python-dotenv), the sqlalchemy DB
#     drivers, httpx[socks/brotli]. uvicorn runs on the pure-Python h11 loop,
#     and dev auto-reload falls back to its poll-based StatReload.
#   - dev/test tooling — pytest lives in requirements-dev.txt.
# Reproduce: python -m venv .venv && .venv/Scripts/python -m pip install -r requirements-dev.txt

aiosqlite==0.22.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
beautifulsoup4==4.15.0
certifi==2026.7.22
click==8.4.2
colorama==0.4.6
exceptiongroup==1.3.1
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
jinja2==3.1.6
markupsafe==3.0.3
pydantic==2.13.4
pydantic-core==2.46.4
pypdf==6.15.0
python-multipart==0.0.32
pyyaml==6.0.3
soupsieve==2.9.2
sqlalchemy==2.0.51
starlette==1.6.0
typing-extensions==4.16.0
typing-inspection==0.4.3
uvicorn==0.52.1